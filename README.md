# -
                var option = {
                    el: "canvas-circle4",
                    width: 75,
                    endValue: 0,
                    time: 0.6,
                    lineWidth: 4
                }
                circle = Progress.render(option);
                circle.update(50);
